# Configuração de exemplo do Jarvis DevOps
# Copie este arquivo para .env e ajuste as configurações conforme necessário

# =============================================================================
# CONFIGURAÇÃO DO SERVIDOR WEB
# =============================================================================

# Host onde o servidor será executado
# Use 0.0.0.0 para aceitar conexões de qualquer IP
# Use 127.0.0.1 para aceitar apenas conexões locais
SERVER_HOST=0.0.0.0

# Porta onde o servidor será executado
SERVER_PORT=8080

# =============================================================================
# CONFIGURAÇÃO DO NGINX
# =============================================================================

# Diretório onde estão localizados os arquivos de configuração do nginx
# Geralmente é /etc/nginx/sites-available no Ubuntu/Debian
# ou /etc/nginx/conf.d no CentOS/RHEL
NGINX_CONFIG_PATH=/etc/nginx/sites-available

# Caminho completo para o binário do nginx
# Execute 'which nginx' para descobrir o caminho no seu sistema
NGINX_BINARY=/usr/sbin/nginx

# Nome do serviço nginx para comandos systemctl
# Geralmente é 'nginx', mas pode variar em algumas distribuições
NGINX_SERVICE_NAME=nginx

# =============================================================================
# SEGURANÇA E AUTENTICAÇÃO
# =============================================================================

# Usuário para autenticação Basic Auth
# IMPORTANTE: Altere este valor em produção
BASIC_AUTH_USER=admin

# Senha para autenticação Basic Auth
# IMPORTANTE: Use uma senha forte em produção
BASIC_AUTH_PASSWORD=admin123

# =============================================================================
# CONFIGURAÇÕES DA APLICAÇÃO
# =============================================================================

# Modo debug - ativa logs detalhados
# Use false em produção
DEBUG=false

# Nível de log: debug, info, warn, error
LOG_LEVEL=info

# =============================================================================
# CONFIGURAÇÕES OPCIONAIS
# =============================================================================

# Tempo limite para operações de arquivo (em segundos)
# FILE_TIMEOUT=30

# Tamanho máximo de arquivo para edição (em bytes)
# MAX_FILE_SIZE=1048576

# Diretório para backups automáticos
# BACKUP_DIR=/var/backups/jarvis-devops

# =============================================================================
# EXEMPLOS PARA DIFERENTES AMBIENTES
# =============================================================================

# Exemplo para ambiente de desenvolvimento local:
# SERVER_HOST=127.0.0.1
# SERVER_PORT=8080
# NGINX_CONFIG_PATH=/usr/local/etc/nginx/servers
# NGINX_BINARY=/usr/local/bin/nginx
# DEBUG=true
# LOG_LEVEL=debug

# Exemplo para produção com nginx padrão Ubuntu:
# SERVER_HOST=127.0.0.1
# SERVER_PORT=8080
# NGINX_CONFIG_PATH=/etc/nginx/sites-available
# NGINX_BINARY=/usr/sbin/nginx
# BASIC_AUTH_USER=nginxadmin
# BASIC_AUTH_PASSWORD=SuaSenhaSeguraAqui123!
# DEBUG=false
# LOG_LEVEL=info

# Exemplo para CentOS/RHEL:
# SERVER_HOST=127.0.0.1
# SERVER_PORT=8080
# NGINX_CONFIG_PATH=/etc/nginx/conf.d
# NGINX_BINARY=/usr/sbin/nginx
# NGINX_SERVICE_NAME=nginx
# BASIC_AUTH_USER=admin
# BASIC_AUTH_PASSWORD=SuaSenhaSegura456!
# DEBUG=false
# LOG_LEVEL=warn
